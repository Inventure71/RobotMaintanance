import { createModelAssetResolver } from '../../../primitives/model-viewer/modelAssetResolver.js';
import { createFleetDerivedStateApi } from '../domain/createFleetDerivedStateApi.js';
import { createFleetSelectionApi } from '../domain/createFleetSelectionApi.js';
import { createFleetMonitorActivityApi } from '../domain/createFleetMonitorActivityApi.js';
import { createFleetCardRenderingApi } from '../domain/createFleetCardRenderingApi.js';
import { createFleetRuntimeStateApi } from '../domain/createFleetRuntimeStateApi.js';
import { createFleetModelViewerApi } from '../domain/createFleetModelViewerApi.js';
import { createFleetWindowingApi } from '../domain/createFleetWindowingApi.js';
import { createFleetDashboardApi } from '../domain/createFleetDashboardApi.js';

export function createFleetFeature(context, maybeEnv) {
  const runtime = maybeEnv ? context : context?.bridge || context?.runtime || context?.services || {};
  const env = maybeEnv || context?.env || context;
      const {
    $, $$, CAN_USE_MODEL_VIEWER, DEFAULT_ROBOT_MODEL_URL, DEFAULT_TEST_DEFINITIONS, DETAIL_TERMINAL_PRESET_IDS,
    FIX_MODE_CONTEXT_DASHBOARD, FIX_MODE_CONTEXT_DETAIL, FLEET_PARALLELISM_DEFAULT, FLEET_PARALLELISM_MAX,
    FLEET_PARALLELISM_MIN, FLEET_PARALLELISM_STORAGE_KEY, FLEET_RUNTIME_ENDPOINT, FLEET_STATIC_ENDPOINT,
    FORCE_TEXT_TEST_ICONS, LOW_BATTERY_WARNING_PERCENT, MANAGE_TABS, MANAGE_TAB_STORAGE_KEY, MANAGE_VIEW_HASH,
    MODAL_SCROLL_LOCK_CLASS, MONITOR_BATTERY_INTERVAL_DEFAULT_SEC, MONITOR_BATTERY_INTERVAL_MAX_SEC,
    MONITOR_BATTERY_INTERVAL_MIN_SEC, MONITOR_MODE_ONLINE_BATTERY, MONITOR_MODE_ONLINE_BATTERY_TOPICS,
    MONITOR_ONLINE_INTERVAL_DEFAULT_SEC, MONITOR_ONLINE_INTERVAL_MAX_SEC, MONITOR_ONLINE_INTERVAL_MIN_SEC, MONITOR_SOURCE,
    MONITOR_TOPICS_INTERVAL_DEFAULT_SEC, MONITOR_TOPICS_INTERVAL_MAX_SEC, MONITOR_TOPICS_INTERVAL_MIN_SEC,
    MONITOR_TOPICS_SOURCE, ONLINE_CHECK_ESTIMATE_ALPHA, ONLINE_CHECK_TIMEOUT_MS, ONLINE_CHECK_UI_BUFFER_MS,
    ONLINE_CHECK_UI_MAX_MS, ONLINE_CHECK_UI_MIN_MS, ONLINE_SORT_BATTERY, ONLINE_SORT_LABELS, ONLINE_SORT_NAME,
    ONLINE_SORT_ORDER, ONLINE_SORT_STATUS, PRESET_COMMANDS, ROBOTS_CONFIG_URL, ROBOT_TYPES, ROBOT_TYPES_CONFIG_URL,
    ROBOT_TYPE_BY_ID, RUNTIME_ALLOWED_SOURCES, RUNTIME_SYNC_INTERVAL_MS, RobotTerminalComponent, TEST_COUNTDOWN_MAX_SECONDS,
    TEST_COUNTDOWN_MIN_SECONDS, TEST_COUNTDOWN_MODE_LABELS, TEST_COUNTDOWN_TICK_MS, TEST_COUNTDOWN_WARNING_TEXT,
    TEST_DEFINITIONS, TEST_ICON_TEXT_FALLBACKS, TEST_STEP_TIMEOUT_MS, WorkflowRecorderComponent,
    activateRecorderTerminalButton, activateTerminalButton, addRobotForm, addRobotMessage, addRobotPasswordInput,
    addRobotPasswordToggle, addRobotSavingHint, addRobotSection, addRobotTypeSelect, applyActionButton, bugReportMessageInput,
    bugReportModal, bugReportStatus, buildApiUrl, cancelBugReportButton, createActionButton, createPageSessionId,
    cycleOnlineSortButton, dashboard, dashboardFixModeActions, dashboardFixModePanel, dashboardFixModeStatus,
    dashboardFixModeSummary, detail, detailFixModeActions, detailFixModePanel, detailFixModeStatus, detailFixModeSummary,
    detailTerminalShell, emptyState, filterError, filterType, hydrateActionButtons, initThemeSwitcher, initVisualFlows,
    manageDeleteFixButton, manageDeleteTestButton, manageFixDescriptionInput, manageFixEditorForm, manageFixEditorStatus,
    manageFixExecuteJsonInput, manageFixIdInput, manageFixLabelInput, manageFixRobotTypeTargets, manageFixesList,
    manageTabButtons, manageTabPanels, manageTabStatus, manageTestChecksJsonInput, manageTestEditorForm,
    manageTestEditorStatus, manageTestExecuteJsonInput, manageTestIdInput, manageTestLabelInput, manageTestRobotTypeTargets,
    manageTestsList, monitorApplyButton, monitorConfigStatus, monitorModeSelect, monitorTopicsIntervalInput, normalizeStatus,
    normalizeText, normalizeTypeId, offlineGrid, offlineSectionTitle, onlineGrid, onlineSectionTitle, recorderAddOutputBtn,
    recorderAddReadBtn, recorderCheckCountBadge, recorderCommandInput, recorderCreateNewTestButton, recorderDefinitionIdInput,
    recorderDefinitionLabelInput, recorderFlowBlocks, recorderLastEditingOutputKey, recorderLastEditingReadBlockId,
    recorderOutputCountBadge, recorderOutputFailDetailsInput, recorderOutputIconInput, recorderOutputKeyInput,
    recorderOutputLabelInput, recorderOutputPassDetailsInput, recorderOutputs, recorderPublishStatus,
    recorderPublishTestButton, recorderReadInputRefSelect, recorderReadKindSelect, recorderReadLinesInput,
    recorderReadNeedleInput, recorderReadNeedlesInput, recorderReadOutputKeySelect, recorderReadRequireAllInput,
    recorderRobotSelect, recorderRunCaptureButton, recorderStateBadge, recorderStatus, recorderStepCountBadge,
    recorderTerminalActivationOverlay, recorderTerminalBadge, recorderTerminalDisplay, recorderTerminalHint,
    recorderTerminalPopReadBtn, recorderTerminalShell, recorderTerminalToolbar, renderBatteryPill, setActionButtonLoading,
    state, submitBugReportButton, terminal, terminalBadge, terminalHint, terminalToolbar, testDebugBody, testDebugClose,
    testDebugModal, testDebugSummary, testDebugTitle, themeSelect, toggleDashboardFixModeButton, toggleDetailFixModeButton,
    } = env;

  const MODEL_RESOLUTION_LOW = 'low';
  const MODEL_RESOLUTION_HIGH = 'high';
  const DEFAULT_MODEL_QUALITY_BASE_PATH = 'assets/models';
  const modelAssetResolver = createModelAssetResolver({
    qualityLevels: [
      { id: MODEL_RESOLUTION_LOW, folder: 'LowRes' },
      { id: MODEL_RESOLUTION_HIGH, folder: 'HighRes' },
    ],
  });
  let renderDashboardHook = () => {};
  let syncSectionToggleButtonsHook = () => {};
  const {
    addRecorderOutputVisual,
    addRecorderReadVisual, appendTerminalLine, appendTerminalPayload, applyMonitorConfig, applyMonitorConfigFromPayload,
    applyRecorderMappings, applyRuntimeRobotPatches, batteryReasonText, buildFixButtonLabel, buildGlobalTestDefinitions,
    buildManageHash, buildTestPreviewText, buildTestPreviewTextForResult, clampFleetParallelism, clampMonitorBatteryInterval,
    clampMonitorOnlineInterval, clampMonitorTopicsInterval, clampOnlineCountdownMs, clearRecorderOutputForm,
    clearRecorderReadForm, closeBugReportModal, closeRecorderTerminalSession, closeTerminalSession, closeTestDebugModal,
    createRobotFromForm, deleteManageFixDefinition, deleteManageTestDefinition, formatConsoleLine, formatEpochSeconds,
    formatRawOutput, formatTestValue, getAutoFixesForType, getConfiguredDefaultTestIds, getDashboardFixCandidates,
    getDefinitionLabel, getDetailFixCandidates, getDetailTerminalPresets, getFallbackTestIconText, getFixModeElements,
    getFleetParallelism, getOnlineCheckCountdownMs, getPersistedManageTab, getRobotDefinitionsForType, getRobotIdsForRun,
    getRobotTypeConfig, getSelectedMappingTypeIds, getSelectedRecorderTypeIds, getTestIconPresentation, getTimestamp,
    getVisibleOfflineRobotIds, getVisibleOnlineRobotIds, haveRuntimeTestsChanged, hideRecorderReadPopover,
    initAddRobotPasswordToggle, initDashboardController, initFleetParallelism, initManageTabs, initMonitorConfigControls,
    initRobotTerminal, initThemeControls, initWorkflowRecorder, isManageViewActive, isTopicsMonitorMode,
    loadDefinitionsSummary, loadFleetRuntimeDelta, loadFleetStaticState, loadMonitorConfig, loadRobotConfig,
    loadRobotTypeConfig, loadRobotsFromBackend, mergeRuntimeRobotsIntoList, normalizeAutoFixDefinition,
    normalizeBatteryReason, normalizeDefinitionsSummary, normalizeIdList, normalizeManageTab, normalizePossibleResult,
    normalizeRobotActivity, normalizeRobotTests, normalizeRobotTypeConfig, normalizeRuntimeRobotEntry,
    normalizeRuntimeTestUpdate, normalizeStepDebug, normalizeTestDebugCollection, normalizeTestDebugResult,
    normalizeTestDefinition, onFilterChange, openBugReportModal, openDetail, openTestDebugModal, parseJsonInput,
    parseManageRoute, patchDetailRuntimeContent, patchRobotTypeMapping, persistManageTab, populateAddRobotTypeOptions,
    populateFilters, publishRecorderAsTest, readRobotField, refreshRobotsFromBackendSnapshot, refreshRuntimeStateFromBackend,
    renderDetail, renderFixModeActionsForContext, renderFixRobotTypeTargets, renderManageDefinitions, renderManageFixesList,
    renderManageTestsList, renderRecorderRobotOptions, renderRecorderRobotTypeTargets, renderTestRobotTypeTargets,
    resolveManageTab, routeFromHash, runAutoFixCandidate, runAutoFixForRobot, runFallbackChecks, runFallbackCommandSimulation,
    runManualTests, runOneRobotOnlineCheck, runOnlineCheckForAllRobots, runRecorderCommandAndCapture, runRobotTestsForRobot,
    stopCurrentJob, renderRobotJobQueueStrip, renderRobotStopCurrentJobButton, runtimeActivityHasSignal,
    saveManageFixDefinition, saveManageTestDefinition, scheduleMonitorParallelismSync, selectAllOfflineRobots,
    selectAllOnlineRobots, selectAllRobots, setActiveManageTab, setAddRobotMessage, setAddRobotPasswordVisibility,
    setBugReportStatus, setFixModeStatus, setFleetOnlineButtonState, setFleetParallelism, setLocationHash,
    setManageEditorStatus, setManageTabStatus, setModelContainerFaultClasses, setMonitorConfigStatus,
    setRecorderTerminalActive, setRobotTypeDefinitions, setRunningButtonState, setTerminalActive, setTerminalInactive,
    showAddRobotPage, showDashboard, slugifyRecorderValue, startRuntimeStateSync, stopRuntimeStateSync, submitBugReport,
    syncFixModePanels, syncFixModeToggleButton, syncFleetParallelismUi, syncModalScrollLock, syncMonitorConfigUi,
    syncMonitorParallelismWithFleet, syncRecorderReadKindFields, syncRecorderReadPopoverVisibility, syncRecorderUiState,
    syncSectionToggleButtons, toggleFixMode, updateCardRuntimeContent, updateFixMappings, updateFleetOnlineSummary,
    updateOnlineCheckEstimateFromResults, updateRobotTestState, updateTestMappings
  } = runtime;


  const {
    normalizeBatteryPercentForSort,
    getRobotBatteryState,
    nonBatteryTestEntries,
    normalizeTagList,
    normalizeOwnerTags,
    normalizePlatformTags,
    getActiveOwnerProfile,
    getTestDefinitionForRobot,
    getDefinitionTagMeta,
    hasTagIntersection,
    matchesDefinitionFilters,
    isUnknownResult,
    getScopedTestEntries,
    computeAggregateState,
    getRobotAggregate,
    getGlobalAggregate,
    getActiveUserAggregate,
    statusFromScore,
    statusSortRank,
    onlineRobotComparator,
    sortOnlineRobots,
    syncOnlineSortButton,
    cycleOnlineSortMode,
    normalizeRobotModelBaseUrl,
    normalizeModelConfig,
    appendModelVersionQuery,
    modelSupportsQuality,
    pickModelConfigForQuality,
    resolveRobotBaseModelUrl,
    resolveRobotModelUrl,
    normalizeRobotData,
  } = createFleetDerivedStateApi({
    cycleOnlineSortButton,
    DEFAULT_MODEL_QUALITY_BASE_PATH,
    DEFAULT_ROBOT_MODEL_URL,
    MODEL_RESOLUTION_HIGH,
    MODEL_RESOLUTION_LOW,
    ONLINE_SORT_BATTERY,
    ONLINE_SORT_LABELS,
    ONLINE_SORT_NAME,
    ONLINE_SORT_ORDER,
    ONLINE_SORT_STATUS,
    getRobotTypeConfig,
    modelAssetResolver,
    normalizeRobotActivity,
    normalizeRobotTests,
    normalizeStatus,
    normalizeTestDebugCollection,
    normalizeText,
    readRobotField,
    renderDashboard: (...args) => renderDashboardHook(...args),
    state,
  });
  const modelViewerApi = createFleetModelViewerApi({
    CAN_USE_MODEL_VIEWER,
    MODEL_RESOLUTION_LOW,
    modelAssetResolver,
    normalizeText,
  });
  const {
    registerLazyModelViewersInNode,
    syncModelViewerRotationForContainer,
  } = modelViewerApi;

  const windowingApi = createFleetWindowingApi({
    initialSize: 120,
    renderDashboard: (...args) => renderDashboardHook(...args),
    stepSize: 80,
    threshold: 160,
  });

  let startTestingCountdownsHook = () => {};
  let stopTestingCountdownsHook = () => {};
  const {
    rebuildRobotIndex,
    setRobots,
    mapRobots,
    robotId,
    getRobotById,
    getSelectedRobotIds,
    getReachableRobotIds,
    isRobotSelected,
    isRobotTesting,
    isRobotSearching,
    isRobotFixing,
    isRobotBusyForOnlineRefresh,
    isRobotAutoSearching,
    setRobotTesting,
    setRobotSearching,
    setRobotFixing,
    estimateTestCountdownMsFromBody,
    normalizeCountdownMs,
    getCountdownLabel,
    getTestingCountdownText,
    shouldUseCompactAutoSearchIndicator,
    buildScanOverlayMarkup,
    buildConnectionCornerIconMarkup,
    formatDurationMs,
  } = createFleetRuntimeStateApi({
    detail,
    ONLINE_CHECK_TIMEOUT_MS,
    TEST_COUNTDOWN_MAX_SECONDS,
    TEST_COUNTDOWN_MIN_SECONDS,
    TEST_COUNTDOWN_MODE_LABELS,
    TEST_STEP_TIMEOUT_MS,
    normalizeStatus,
    normalizeText,
    renderDashboard: (...args) => renderDashboardHook(...args),
    renderDetail,
    startTestingCountdowns: (...args) => startTestingCountdownsHook(...args),
    state,
    stopTestingCountdowns: (...args) => stopTestingCountdownsHook(...args),
  });

  const {
    buildLastFullTestPillLabel,
    formatLastFullTestTimestamp,
    getCountdownNodes,
    getMonitorBatteryIntervalMs,
    getMonitorOnlineIntervalMs,
    getMonitorTopicsIntervalMs,
    invalidateCountdownNodeCache,
    normalizeCheckedAtMs,
    refreshTestingCountdowns,
    setFleetOnlineButtonIdleLabel,
    setRobotSearchingBulk,
    startOnlineRefreshStatusTimer,
    startTestingCountdowns,
    stopOnlineRefreshStatusTimer,
    stopTestingCountdowns,
    syncAutoMonitorRefreshState,
    syncAutomatedRobotActivityFromState,
    updateFleetOnlineRefreshStatus,
  } = createFleetMonitorActivityApi({
    $,
    MONITOR_BATTERY_INTERVAL_MIN_SEC,
    MONITOR_ONLINE_INTERVAL_MIN_SEC,
    MONITOR_SOURCE,
    MONITOR_TOPICS_INTERVAL_MIN_SEC,
    MONITOR_TOPICS_SOURCE,
    ONLINE_CHECK_TIMEOUT_MS,
    TEST_COUNTDOWN_TICK_MS,
    TEST_COUNTDOWN_WARNING_TEXT,
    TEST_STEP_TIMEOUT_MS,
    clampMonitorBatteryInterval,
    clampMonitorOnlineInterval,
    clampMonitorTopicsInterval,
    detail,
    formatDurationMs,
    getCountdownLabel,
    getRobotBatteryState,
    isTopicsMonitorMode,
    normalizeCountdownMs,
    normalizeRobotActivity,
    normalizeText,
    renderDashboard: (...args) => renderDashboardHook(...args),
    renderDetail,
    robotId,
    state,
  });

  const {
    addRobotIdsToSelection,
    areAllRobotIdsSelected,
    filterRobotIdsToSingleType,
    getRobotTypeKey,
    getRunSelectedButtonIdleLabel,
    getSingleRobotTypeKeyForIds,
    hasMixedRobotTypesForIds,
    removeRobotIdsFromSelection,
    selectRobotIds,
    setRobotSelection,
    syncGlobalSelectionButton,
    syncRunSelectedButtonLabel,
    syncSelectionUi,
    updateSelectionSummary,
  } = createFleetSelectionApi({
    FIX_MODE_CONTEXT_DASHBOARD,
    $,
    $$,
    applyActionButton,
    getRobotById,
    normalizeText,
    normalizeTypeId,
    renderFixModeActionsForContext: (...args) => {
      if (typeof runtime.renderFixModeActionsForContext === 'function') {
        return runtime.renderFixModeActionsForContext(...args);
      }
      return undefined;
    },
    robotId,
    setRunningButtonState: (...args) => {
      if (typeof runtime.setRunningButtonState === 'function') {
        return runtime.setRunningButtonState(...args);
      }
      return undefined;
    },
    state,
    syncSectionToggleButtons: (...args) => syncSectionToggleButtonsHook(...args),
  });

  let getStatusChipToneHook = (statusKey) => {
    if (statusKey === 'critical') return { css: 'err', text: 'Critical' };
    if (statusKey === 'warning') return { css: 'warn', text: 'Warning' };
    if (statusKey === 'unknown') return { css: 'neutral', text: 'Unknown' };
    if (statusKey === 'na') return { css: 'neutral', text: 'N/A' };
    return { css: 'ok', text: 'Healthy' };
  };
  const {
    buildRobotTypeDivider,
    buildRobotModelContainer,
    buildRobotModelMarkup,
    describeRobotCard,
    groupRobotsByType,
    issueSummary,
    renderCard,
    renderRobotTypeGroups,
    robotModelMarkup,
    statusChip,
    syncRobotCard,
  } = createFleetCardRenderingApi({
    CAN_USE_MODEL_VIEWER,
    MODEL_RESOLUTION_LOW,
    appendTerminalLine,
    buildConnectionCornerIconMarkup,
    buildLastFullTestPillLabel,
    buildScanOverlayMarkup,
    documentRef: document,
    getActiveUserAggregate,
    getDefinitionLabel,
    getGlobalAggregate,
    getRobotBatteryState,
    getRobotTypeConfig,
    getScopedTestEntries,
    getStatusChipTone: (...args) => getStatusChipToneHook(...args),
    getTestingCountdownText,
    hydrateActionButtons,
    isRobotFixing,
    isRobotSearching,
    isRobotSelected,
    isRobotTesting,
    isUnknownResult,
    modelAssetResolver,
    normalizeStatus,
    normalizeText,
    normalizeTypeId,
    openDetail,
    registerLazyModelViewersInNode,
    renderBatteryPill,
    renderRobotJobQueueStrip,
    renderRobotStopCurrentJobButton,
    resolveRobotBaseModelUrl,
    robotId,
    setRobotSelection,
    shouldUseCompactAutoSearchIndicator,
    stopCurrentJob,
    syncModelViewerRotationForContainer,
  });
  const {
    applyFilters,
    updateKPIs,
    renderDashboard,
    applyDashboardMetaFromVisible,
    patchDashboardForChangedRobots,
    getStatusChipTone,
    queryCardByRobotId,
  } = createFleetDashboardApi({
    $,
    FIX_MODE_CONTEXT_DASHBOARD,
    dashboard,
    emptyState,
    getScopedTestEntries,
    getRobotById,
    hydrateActionButtons,
    invalidateCountdownNodeCache,
    isUnknownResult,
    normalizeStatus,
    normalizeText,
    offlineGrid,
    offlineSectionTitle,
    onlineGrid,
    onlineSectionTitle,
    renderFixModeActionsForContext: (...args) => {
      if (typeof runtime.renderFixModeActionsForContext === 'function') {
        return runtime.renderFixModeActionsForContext(...args);
      }
      return undefined;
    },
    renderRobotTypeGroups,
    robotId,
    sortOnlineRobots,
    state,
    statusFromScore,
    syncFixModePanels: (...args) => {
      if (typeof runtime.syncFixModePanels === 'function') {
        return runtime.syncFixModePanels(...args);
      }
      return undefined;
    },
    syncRobotCard,
    syncSectionToggleButtons: (...args) => syncSectionToggleButtonsHook(...args),
    updateSelectionSummary,
    windowingApi,
  });
  renderDashboardHook = (...args) => renderDashboard(...args);
  syncSectionToggleButtonsHook = (...args) => {
    if (typeof runtime.syncSectionToggleButtons === 'function') {
      return runtime.syncSectionToggleButtons(...args);
    }
    return undefined;
  };
  getStatusChipToneHook = (...args) => getStatusChipTone(...args);
  startTestingCountdownsHook = (...args) => startTestingCountdowns(...args);
  stopTestingCountdownsHook = (...args) => stopTestingCountdowns(...args);

  return {
    init() {},
    normalizeBatteryPercentForSort,
    getRobotBatteryState,
    nonBatteryTestEntries,
    normalizeTagList,
    normalizeOwnerTags,
    normalizePlatformTags,
    getActiveOwnerProfile,
    getTestDefinitionForRobot,
    getDefinitionTagMeta,
    hasTagIntersection,
    matchesDefinitionFilters,
    isUnknownResult,
    getScopedTestEntries,
    computeAggregateState,
    getRobotAggregate,
    getGlobalAggregate,
    getActiveUserAggregate,
    statusFromScore,
    statusSortRank,
    onlineRobotComparator,
    sortOnlineRobots,
    syncOnlineSortButton,
    cycleOnlineSortMode,
    resolveRobotModelUrl,
    normalizeRobotData,
    rebuildRobotIndex,
    setRobots,
    mapRobots,
    robotId,
    getRobotById,
    getSelectedRobotIds,
    getReachableRobotIds,
    isRobotSelected,
    isRobotTesting,
    isRobotSearching,
    isRobotFixing,
    isRobotBusyForOnlineRefresh,
    isRobotAutoSearching,
    setRobotTesting,
    setRobotSearching,
    setRobotFixing,
    estimateTestCountdownMsFromBody,
    normalizeCountdownMs,
    getCountdownLabel,
    getTestingCountdownText,
    shouldUseCompactAutoSearchIndicator,
    buildScanOverlayMarkup,
    buildConnectionCornerIconMarkup,
    syncModelViewerRotationForContainer,
    formatDurationMs,
    getMonitorOnlineIntervalMs,
    getMonitorBatteryIntervalMs,
    getMonitorTopicsIntervalMs,
    normalizeCheckedAtMs,
    formatLastFullTestTimestamp,
    buildLastFullTestPillLabel,
    syncAutoMonitorRefreshState,
    stopOnlineRefreshStatusTimer,
    setFleetOnlineButtonIdleLabel,
    updateFleetOnlineRefreshStatus,
    startOnlineRefreshStatusTimer,
    invalidateCountdownNodeCache,
    getCountdownNodes,
    refreshTestingCountdowns,
    startTestingCountdowns,
    stopTestingCountdowns,
    setRobotSearchingBulk,
    syncAutomatedRobotActivityFromState,
    getRobotTypeKey,
    getSingleRobotTypeKeyForIds,
    hasMixedRobotTypesForIds,
    filterRobotIdsToSingleType,
    setRobotSelection,
    selectRobotIds,
    addRobotIdsToSelection,
    removeRobotIdsFromSelection,
    areAllRobotIdsSelected,
    updateSelectionSummary,
    getRunSelectedButtonIdleLabel,
    syncRunSelectedButtonLabel,
    syncSelectionUi,
    syncGlobalSelectionButton,
    statusChip,
    robotModelMarkup,
    buildRobotModelMarkup,
    buildRobotModelContainer,
    issueSummary,
    groupRobotsByType,
    buildRobotTypeDivider,
    renderRobotTypeGroups,
    renderCard,
    applyFilters,
    updateKPIs,
    renderDashboard,
    applyDashboardMetaFromVisible,
    patchDashboardForChangedRobots,
    getStatusChipTone,
    queryCardByRobotId,
  };
}
